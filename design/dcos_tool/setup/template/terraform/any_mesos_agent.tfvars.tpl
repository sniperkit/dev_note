bootstrap_host = "$BOOTSTRAP_HOST"
bootstrap_web_port = "$BOOTSTRAP_WEB_PORT"
mesos_agent_list = [ "$MESOS_AGENT_LIST" ]
num_of_mesos_agent = "${MESOS_AGENT_COUNT}"
mesos_agent_username = "${MESOS_AGENT_USERNAME}"
mesos_agent_password = "${MESOS_AGENT_PASSWORD}"