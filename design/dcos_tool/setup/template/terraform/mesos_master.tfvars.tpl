bootstrap_host = "$BOOTSTRAP_HOST"
bootstrap_web_port = "$BOOTSTRAP_WEB_PORT"
mesos_master_list = [ "$MESOS_MASTER_LIST" ]
num_of_mesos_master = "${MESOS_MASTER_COUNT}"
mesos_master_username = "${MESOS_MASTER_USERNAME}"
mesos_master_password = "${MESOS_MASTER_PASSWORD}"